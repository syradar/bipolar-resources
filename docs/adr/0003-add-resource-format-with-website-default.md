# Add resource format with a website default

Resources are no longer always URL-backed because the catalogue may include books, videos, podcasts and places. We will add a controlled resource format and treat missing format values as `website` during migration, so existing Markdown and Contentful entries keep working while the CMS model is expanded and backfilled.
